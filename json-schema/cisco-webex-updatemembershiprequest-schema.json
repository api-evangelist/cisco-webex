{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "#/components/schemas/UpdateMembershipRequest",
  "title": "UpdateMembershipRequest",
  "type": "object",
  "required": [
    "isModerator"
  ],
  "properties": {
    "isModerator": {
      "type": "boolean",
      "description": "Whether the person is a room moderator."
    },
    "isRoomHidden": {
      "type": "boolean",
      "description": "Whether to hide the direct space."
    }
  }
}